@model DanAutoPower.Models.Car

@{
    ViewData["Title"] = "Изтриване";
}

<div class="container py-5">
    <div class="card shadow-lg p-4">
        <h2 class="text-danger mb-3">Изтриване на обява</h2>

        <div class="alert alert-warning">
            <strong>Сигурни ли сте, че искате да изтриете тази обява?</strong>
        </div>

        <div class="row mb-3">
            <div class="col-md-6">
                <label class="form-label fw-bold">Марка:</label>
                <div class="form-control-plaintext">@Model.Make</div>
            </div>
            <div class="col-md-6">
                <label class="form-label fw-bold">Модел:</label>
                <div class="form-control-plaintext">@Model.Model</div>
            </div>
            <div class="col-md-6">
                <label class="form-label fw-bold">Година:</label>
                <div class="form-control-plaintext">@Model.Year</div>
            </div>
            <div class="col-md-6">
                <label class="form-label fw-bold">Цена:</label>
                <div class="form-control-plaintext text-danger fw-bold">@Model.Price.ToString("N0") лв.</div>
            </div>
        </div>

        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <div class="d-flex gap-3">
                <button type="submit" class="btn btn-danger">🗑️ Изтрий</button>
                <a asp-action="Index" class="btn btn-outline-secondary">⬅ Отказ</a>
            </div>
        </form>
    </div>
</div>
